export type StreamInfo = {
    abr: string,
    codec: string,
    fileSize: number,
    title: string,
    url: string,
    mimeType: string,
    expiresInSeconds: number,
    expiresAt: Date,
    thumbnail: string,
}